specVersion: "v1beta"
serviceId: "gpscam"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gpscam-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
